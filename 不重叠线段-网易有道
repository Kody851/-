在一位坐标系，输入几个线段的端点，求所有不重复的段的两段端点。
例如：输入[1,5] [3,8] [7,10]  则输出 [1,3] [5,7] [8,10]
知识点：
pair<int,int>b(1,2);
vector<pair<int,int>>a;
a.push_back(make_pair(3,4));
a.push_back(make_pair(5,6));
cout<<a[0].first;//3
cout<<a[1].second;//6
for(auto i:a) cout<<i.first; //3 5
vector<pair<int,int>>::iterator iter;
for(iter=a.begin();iter!=a.end();iter++) cout<<(*iter).first; //3 5

void FindNorepetirionSegment(const vector<pair<int,int>>inputs, vector<pair<int,int>>&outputs){
